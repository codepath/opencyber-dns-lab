# Run-smoke config for the DNS Security lab (../_smoke-test.sh, rubric TECH-4).
# Probe = the resolver answers the bank's real record (pre-poison baseline).
PROBE_CMD='dig @localhost A login.northwind-bank.test +short'
PROBE_EXPECT='203.0.113.10'
# The decoded flag plaintext must never appear un-encoded anywhere in the repo.
BANNED_EXTRA='dns_exfil_hidden'
